# Application Configuration
SPRING_PROFILES_ACTIVE=dev
APP_PORT=8080

# PostgreSQL Database Configuration
POSTGRES_DB=ai_studio
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# JWT Configuration
# IMPORTANT: Use a strong, random secret key in production (at least 256 bits)
JWT_SECRET=your-super-secret-key-change-in-production-minimum-256-bits-long

# AI Service API Keys
# Replace with your actual API keys
EMBEDDING_API_KEY=your-embedding-api-key-here
LLM_API_KEY=your-llm-api-key-here

# File Storage Configuration
UPLOAD_DIR=/app/uploads

# Monitoring and Metrics
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# Production Only Settings
# Uncomment and configure these for production deployment

# Registry URL for Docker images (production)
# REGISTRY_URL=your-registry-url/

# Image tag (production)
# TAG=latest

# Production database configuration
# POSTGRES_DB=ai_studio_prod
# POSTGRES_USER=ai_studio_user
# POSTGRES_PASSWORD=strong-password-here

# Production JWT secret (MUST be changed)
# JWT_SECRET=production-secret-key-minimum-256-bits-long-random-string

# Production API keys
# EMBEDDING_API_KEY=prod-embedding-api-key
# LLM_API_KEY=prod-llm-api-key

# Production storage paths
# UPLOAD_DIR=/var/lib/ai-studio/uploads

# Production Grafana credentials
# GRAFANA_ADMIN_USER=admin
# GRAFANA_ADMIN_PASSWORD=strong-grafana-password

# Resource Limits (Production)
# JAVA_OPTS=-Xms2g -Xmx4g -XX:+UseG1GC -XX:MaxRAMPercentage=75.0

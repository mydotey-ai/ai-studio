# 数据可视化仪表盘设计文档

> 创建时间: 2026-01-23
> 状态: 待实施

## 一、功能概述

AI Studio 数据可视化仪表盘为管理员提供系统全局视图，展示关键业务指标、资源使用情况和用户活动分析。

### 核心价值

- **快速决策**: 实时掌握系统运行状态
- **趋势洞察**: 发现使用模式和增长趋势
- **问题定位**: 快速识别异常和瓶颈
- **资源优化**: 了解资源分配和使用情况

---

## 二、功能模块

### 2.1 统计卡片区域

显示 6 个核心业务指标卡片：

| 卡片 | 图标 | 主指标 | 趋势 | 副指标 |
|------|------|--------|------|--------|
| 知识库 | FolderOpened | 总数 | 周增长率 | 活跃/归档分布 |
| Agent | User | 总数 | 月新增数 | ReAct/Workflow |
| 聊天机器人 | ChatDotSquare | 总数 | 总对话数 | 已发布/草稿 |
| 文档 | Document | 总数 | 处理状态 | 文档大小统计 |
| 用户 | User | 总数 | 活跃用户 | 管理员/普通用户 |
| 存储 | Files | 使用量 | 文件数量 | LOCAL/OSS/S3 分布 |

**卡片特性：**
- 响应式布局 (移动端 2 列, 平板 3 列, 桌面 6 列)
- 悬停动画效果
- 点击跳转到详情页
- 趋势指示器 (↑ 绿色增长 / ↓ 红色下降)

### 2.2 图表可视化区域

#### 左侧图表区 (60% 宽度)

**a) 资源分布环形图**
- 数据内容: 各模块状态分布
- 图表类型: 环形饼图 (doughnut chart)
- 交互功能: 点击图例隐藏/显示
- 动画效果: 渐入式展开
- 工具提示: 显示数值和百分比

**b) 使用趋势折线图**
- X 轴: 时间 (7 天 / 30 天可切换)
- Y 轴左: API 调用次数
- Y 轴右: 活跃用户数
- 标记点: 重要事件 (如系统部署)
- 平滑曲线: 贝塞尔曲线插值
- 数据点: 悬停显示详细数值

#### 右侧图表区 (40% 宽度)

**c) 最近活动时间线**
- 数据来源: 审计日志 API
- 显示条数: 最近 10 条
- 时间线样式: 垂直时间轴
- 颜色编码: 按操作类型着色
- 交互: 点击查看完整日志详情
- 自动刷新: 30 秒更新

**d) 系统健康仪表盘**
- 指标: API 成功率、平均响应时间、错误率
- 仪表盘类型: 仪表图 (gauge chart)
- 颜色区间: 绿 (>95%) / 黄 (85-95%) / 红 (<85%)
- 实时更新: 1 分钟刷新周期
- 告警阈值: 可配置

### 2.3 快捷操作区域

**常用操作按钮组：**
- ➕ 新建知识库
- 🤖 创建 Agent
- 💬 创建聊天机器人
- 📊 查看详细报告

---

## 三、技术架构

### 3.1 前端组件结构

```
DashboardView.vue (主视图)
├── StatCard.vue (统计卡片组件)
├── ResourcePieChart.vue (资源分布饼图)
├── TrendLineChart.vue (趋势折线图)
├── ActivityTimeline.vue (活动时间线)
├── HealthGauge.vue (健康状态仪表盘)
└── types/
    └── dashboard.ts (仪表盘类型定义)
```

### 3.2 技术栈

**新增依赖：**
```json
{
  "echarts": "^5.5.0",
  "vue-echarts": "^6.6.0"
}
```

**核心库：**
- **图表**: Apache ECharts 5.5+
- **Vue 集成**: vue-echarts (官方维护)
- **UI 框架**: Element Plus 2.13+
- **状态管理**: Pinia 2.3+ (可选)
- **HTTP 客户端**: Axios 1.13+

### 3.3 数据获取策略

#### 前端 API 设计

```typescript
// frontend/src/api/dashboard.ts
import request from './request'
import type {
  DashboardStatistics,
  TrendData,
  Activity,
  HealthStatus
} from '@/types/dashboard'

export const dashboardApi = {
  // 获取统计数据汇总
  getStatistics() {
    return request<DashboardStatistics, never>({
      url: '/dashboard/statistics',
      method: 'get'
    })
  },

  // 获取趋势数据
  getTrends(days: number = 7) {
    return request<TrendData[], never>({
      url: `/dashboard/trends?days=${days}`,
      method: 'get'
    })
  },

  // 获取最近活动
  getRecentActivities(limit: number = 10) {
    return request<Activity[], never>({
      url: `/dashboard/activities?limit=${limit}`,
      method: 'get'
    })
  },

  // 获取系统健康状态
  getHealthStatus() {
    return request<HealthStatus, never>({
      url: '/dashboard/health',
      method: 'get'
    })
  }
}
```

#### 后端 API 设计 (需实现)

**Controller:**
```java
@RestController
@RequestMapping("/api/dashboard")
@RequiredArgsConstructor
@Tag(name = "仪表盘", description = "数据可视化仪表盘相关接口")
public class DashboardController {

    @GetMapping("/statistics")
    @Operation(summary = "获取统计数据汇总")
    public ApiResponse<DashboardStatisticsDTO> getStatistics();

    @GetMapping("/trends")
    @Operation(summary = "获取趋势数据")
    public ApiResponse<List<TrendDataDTO>> getTrends(
        @RequestParam(defaultValue = "7") int days
    );

    @GetMapping("/activities")
    @Operation(summary = "获取最近活动")
    public ApiResponse<List<ActivityDTO>> getRecentActivities(
        @RequestParam(defaultValue = "10") int limit
    );

    @GetMapping("/health")
    @Operation(summary = "获取系统健康状态")
    public ApiResponse<HealthStatusDTO> getHealthStatus();
}
```

**DTOs:**
```java
// DashboardStatisticsDTO.java
@Data
public class DashboardStatisticsDTO {
    private KnowledgeBaseStats knowledgeBases;
    private AgentStats agents;
    private ChatbotStats chatbots;
    private DocumentStats documents;
    private UserStats users;
    private StorageStats storage;
}

// TrendDataDTO.java
@Data
public class TrendDataDTO {
    private String date;
    private Long apiCalls;
    private Integer activeUsers;
}

// ActivityDTO.java
@Data
public class ActivityDTO {
    private Long id;
    private String action;
    private String resourceType;
    private String username;
    private Instant createdAt;
}

// HealthStatusDTO.java
@Data
public class HealthStatusDTO {
    private Double apiSuccessRate;
    private Long avgResponseTime; // ms
    private Double errorRate;
}
```

### 3.4 数据刷新策略

**方案：智能分层刷新**

| 数据类型 | 刷新频率 | 缓存策略 | 实现方式 |
|---------|---------|---------|---------|
| 统计卡片 | 30 秒 | Redis 5 分钟 | 定时器 + SWR |
| 趋势图表 | 手动 | Redis 30 分钟 | 刷新按钮 |
| 活动时间线 | 30 秒 | 无缓存 | 定时器 |
| 健康状态 | 60 秒 | Redis 1 分钟 | 定时器 |

**缓存键设计：**
```
dashboard:statistics:v1
dashboard:trends:{days}:v1
dashboard:health:v1
```

---

## 四、视觉设计

### 4.1 页面布局

```
┌─────────────────────────────────────────────────┐
│  AI Studio 概览          [刷新] [导出]          │
├──────────┬──────────┬──────────┬──────────┬──────────┬──────────┐
│ 📚 知识库 │ 🤖 Agent │ 💬 聊天   │ 📄 文档  │ 👥 用户  │ 🗄️ 存储  │
│   128    │   45     │   23     │  1,234   │   156    │  45.2GB  │
│  ↑ 12%   │  ↑ 3     │  ↓ 5%    │  ↑ 8%    │  ↑ 15    │  ↑ 2%    │
├──────────┴──────────┴──────────┴──────────┴──────────┴──────────┤
│  资源分布 (环形图)          │  最近活动 (时间线)         │
│                            │                            │
│                            │                            │
├────────────────────────────┴────────────────────────────┤
│  使用趋势 (折线图)          │  系统健康 (仪表盘)         │
│                            │                            │
│                            │                            │
└────────────────────────────┴────────────────────────────┘
```

### 4.2 颜色方案

**Element Plus 色系：**
- Primary: #409EFF (蓝色)
- Success: #67C23A (绿色)
- Warning: #E6A23C (橙色)
- Danger: #F56C6C (红色)
- Info: #909399 (灰色)

**图表色板：**
- 知识库: #5470C6
- Agent: #91CC75
- 聊天机器人: #FAC858
- 文档: #EE6666
- 用户: #73C0DE
- 存储: #3BA272

### 4.3 组件样式

**统计卡片：**
- 背景色: #FFFFFF
- 阴影: 0 2px 12px rgba(0,0,0,0.1)
- 圆角: 8px
- 内边距: 20px
- 最小高度: 120px
- 悬停效果: translateY(-2px)

**图表容器：**
- 背景色: #FFFFFF
- 阴影: 0 2px 8px rgba(0,0,0,0.08)
- 圆角: 8px
- 内边距: 20px
- 最小高度:
  - 饼图/仪表盘: 300px
  - 折线图: 350px
  - 时间线: 400px

---

## 五、实现计划

### 5.1 Phase 1: MVP (本次实施)

**目标：** 实现核心统计功能和基础图表

**任务列表：**
1. ✅ 安装 ECharts 和 vue-echarts
2. ✅ 创建类型定义 (dashboard.ts)
3. ✅ 实现 StatCard 组件
4. ✅ 实现 6 个统计卡片
5. ✅ 实现后端 Dashboard API (4 个端点)
6. ✅ 实现资源分布环形图
7. ✅ 实现最近活动时间线
8. ✅ 实现数据刷新逻辑
9. ✅ 响应式布局适配
10. ✅ 测试和优化

**预计工作量：** 4-6 小时

**成功标准：**
- 所有统计卡片正确显示数据
- 图表可正常渲染和交互
- 数据自动刷新功能正常
- 响应式布局在移动端正常

### 5.2 Phase 2: 增强功能 (后续迭代)

**任务列表：**
- ⏳ 使用趋势折线图
- ⏳ 系统健康仪表盘
- ⏳ 数据筛选器 (时间范围选择)
- ⏳ 数据导出功能 (CSV/Excel)
- ⏳ 自定义仪表盘布局

### 5.3 Phase 3: 高级功能 (未来扩展)

**任务列表：**
- ⏳ 实时数据推送 (WebSocket)
- ⏳ 自定义时间范围查询
- ⏳ 对比分析 (同比/环比)
- ⏳ 告警通知系统
- ⏳ 多租户数据隔离

---

## 六、性能优化

### 6.1 前端优化

**代码层面：**
- 组件懒加载 (defineAsyncComponent)
- 图表按需加载 (ECharts 按需引入)
- 防抖/节流 (resize、scroll 事件)
- 虚拟滚动 (大量数据场景)

**缓存策略：**
- SWR 模式 (stale-while-revalidate)
- 组件缓存 (keep-alive)
- 请求去重 (相同请求合并)

### 6.2 后端优化

**查询优化：**
- 统计查询使用 COUNT 和 GROUP BY
- 趋势数据使用日期索引
- 活动查询利用审计日志索引

**缓存策略：**
- Redis 缓存统计数据 (5 分钟 TTL)
- 趋势数据缓存 (30 分钟 TTL)
- 健康状态缓存 (1 分钟 TTL)

---

## 七、测试计划

### 7.1 单元测试

**前端组件测试：**
- StatCard 组件渲染测试
- 图表组件交互测试
- API 调用测试

**后端服务测试：**
- DashboardService 测试
- 统计数据计算测试
- 趋势数据聚合测试

### 7.2 集成测试

**端到端测试场景：**
1. 仪表盘页面加载
2. 统计卡片数据展示
3. 图表交互功能
4. 数据刷新功能
5. 响应式布局

### 7.3 性能测试

**指标：**
- 首屏加载时间 < 2 秒
- 图表渲染时间 < 500ms
- 数据刷新延迟 < 1 秒
- API 响应时间 < 200ms (缓存命中)

---

## 八、风险和挑战

### 8.1 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| ECharts 性能问题 | 大数据量卡顿 | 数据抽样、虚拟滚动 |
| 后端 API 响应慢 | 用户体验差 | Redis 缓存、异步加载 |
| 图表适配问题 | 移动端显示异常 | 响应式配置、媒体查询 |

### 8.2 业务风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 统计数据不准确 | 错误决策 | 数据验证、单元测试 |
| 实时数据延迟 | 信息过时 | 合理设置刷新频率 |
| 权限控制不当 | 数据泄露 | 添加权限验证 |

---

## 九、后续优化方向

1. **个性化仪表盘** - 用户自定义显示内容
2. **预测分析** - 基于历史数据预测趋势
3. **对比分析** - 同比、环比分析功能
4. **智能告警** - 异常检测和通知
5. **导出报告** - PDF/Excel 报告生成
6. **多租户支持** - 按组织隔离数据
7. **实时监控** - WebSocket 推送实时数据
8. **移动端优化** - PWA 支持、离线可用

---

## 十、参考资源

**技术文档：**
- [ECharts 官方文档](https://echarts.apache.org/zh/)
- [vue-echarts 使用指南](https://github.com/ecomfe/vue-echarts)
- [Element Plus 组件库](https://element-plus.org/)

**设计参考：**
- [Grafana 仪表盘设计](https://grafana.com/)
- [Kibana Dashboard](https://www.elastic.co/kibana/)
- [Ant Design Pro 数据看板](https://pro.ant.design/)

---

**文档版本:** v1.0
**最后更新:** 2026-01-23
